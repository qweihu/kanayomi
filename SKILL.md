---
name: utayomi
version: 1.0.0
description: 日语歌词注音工具。输入日语歌词文本，输出带<ruby>振假名标注的注音版本，支持平假名和罗马音两种模式。
---

# Utayomi

## 核心功能

将日语歌词中的汉字标注读音，支持：
- **平假名模式**：汉字标注平假名（如 `<ruby>夢<rt>ゆめ</rt></ruby>`）
- **罗马音模式**：所有日文字符标注罗马音（如 `<ruby>夢<rt>yume</rt></ruby>`）

## 输入处理

### 场景 A：用户直接提供歌词
直接调用注音脚本：
```bash
echo "歌词文本" | python scripts/utayomi_core.py
```

### 场景 B：用户提供 URL
使用工具或 curl 抓取网页源码，**由你（Agent）提取并清洗出纯净的日文歌词文本后**，再调用注音脚本：
```bash
echo "清洗后的纯歌词文本" | python scripts/utayomi_core.py
```

## 输出格式

- **首行加粗规则**：如果输出文本的首行包含歌曲名称和歌手名，**必须将整个首行加粗**（例如：`**歌曲: {歌名} / {歌手名}**`）。
- **正文精简规则**：生成出来的 Markdown 正文**不要显示**“处理结果”“数据来源”“注音方式”“翻译语言”“保存路径”等摘要区块；这些信息只用于对话回复或内部过程判断，不进入文件正文。

### 头部信息（固定格式）
```
**歌曲: {歌名} / {歌手名}**
```

### 歌词排版
- 一行日文歌词，紧跟一行中文翻译
- 每组之间空一行
- 日文歌词中的汉字必须全部标注 `<ruby>`

示例：
```
<ruby>夢<rt>ゆめ</rt></ruby>ならばどれほどよかったでしょう
如果这是梦，那该有多好啊

<ruby>未<rt>いま</rt></ruby>だにあなたのことを<ruby>夢<rt>ゆめ</rt></ruby>にみる
至今仍会在梦中见到你
```

## 质检规则

输出前必须检查：
- [ ] 所有日文汉字都有 `<ruby>` 标注
- [ ] 标题行（如有）已标注
- [ ] 每句日文下方都有对应中文翻译
- [ ] 没有网页噪音（广告、按钮文字等）

## 禁止事项

- 不要生成歌曲赏析、情感解读
- 不要在生成的歌词 Markdown 正文中使用 emoji 装饰
- 不要添加"质检确认"等自检说明
- 不要在生成的歌词 Markdown 正文中输出“处理结果”或其明细字段
- 不要输出中间过程，只输出最终结果

## 执行步骤

1. **获取歌词**：根据用户输入方式（直接文本/URL）获取歌词
2. **调用注音**：`python scripts/utayomi_core.py [--romaji]`
3. **逐句翻译**：将注音后的日文逐句翻译成简体中文
4. **排版输出**：按固定格式生成加粗标题（如有）+ 中日对照歌词；不要在 Markdown 正文中附带处理结果摘要
5. **保存文件**：尝试写入 `{歌名}-{歌手名}.md`。如果该文件已存在，请在文件名中追加当前的年月日时分（如 `YYYYMMDDHHmm`），生成诸如 `{歌名}-{歌手名}-202604191530.md` 的文件。
6. **主动告知用户**：文件保存成功后，**务必在对话中主动回复用户**："处理完成！歌词已保存在：【文件的完整绝对路径】"
7. **最终质检**：确认无漏标后输出
